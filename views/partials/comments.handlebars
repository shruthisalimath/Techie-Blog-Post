<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="meta">
            posted by {{user.userName}} on {{format_date created_at}}
        </div>
        <div class="text">
            {{feedback}}
        </div>
    </section>
    {{/each}}
</div>