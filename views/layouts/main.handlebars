<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" contet="width=device-width, initial-scale=1.0">
    <title>Tech-Blog</title>
    <link rel="stylesheet" type="text/css" href="/stylesheets/style.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.4/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</head>

<body>
    <div class="wrapper">
        <header id="top-header">
            <h1 class="px-3  has-text-weight-bold" id="main-heading">Tech-Blog</h1>
            <nav class="navbar mt-2" id="home-login">

               <a href="/" class="navItem is-hoverable pt-1" id="homepage">Home</a>

                {{#if loggedIn}}
                <a href="/dashboard" id="homepage">Dashboard</a>
                <button id="logout" class="btn-style">Logout</button></a>
                {{else}}
               
                <a href="/dashboard">Dashboard</a>
                 <a href="/login" class="button is-primary">Login</a>
                {{/if}}
    </div>
    </nav>
    </header>

    <main>
        <!--- Render the sub layoyt --->
        {{{ body }}}
    </main>
    </div>
    <!--- Render script for logged in users only --->
    {{#if loggedIn}}

    <script src="/javascript/logout.js"></script>
    {{/if}}

</body>

</html>